@model IEnumerable<Document>

@{
    ViewData["Title"] = "Home Page";
}

<div class="menu-container">
</div>

<div class="container p-3">
    <div class="row">
        <div class="col-6">
            <h2 class="text-primary">Documents List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Documents" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-square"></i> &nbsp;Upload File
            </a>
        </div>
    </div>

    <br /><br />

   @* <table class="table table-bordered table-striped" style="width: 100%">
        <thead>
            <tr>
                <th style="width: 25%"></th>
                <th style="width: 15%" class="text-center">Title</th>
                <th style="width: 20%" class="text-center">Description</th>
                <th style="width: 20%" class="text-center">Date Uploaded</th>
                <th style="width: 20%" class="text-center">Actions</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <img src="@item.FileImage" class="img-thumbnail" />
                    </td>
                    <td align="center" valign="middle">@item.Title</td>
                    <td align="center" valign="middle">@item.Description</td>
                    <td align="center" valign="middle">@item.DateUploaded</td>
                </tr>
            }
        </tbody>
    </table>*@
</div>